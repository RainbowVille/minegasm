name: Build 1.20

on:
  push:
    branches: [ main ]
    paths:
      - '1.20/**'
      - '.github/workflows/gradle-1.20.yml'
  pull_request:
    branches: [ main ]
    paths:
      - '1.20/**'
      - '.github/workflows/gradle-1.20.yml'

jobs:
  gradle:
    uses: ./.github/workflows/gradle.yml
    with:
      version: 1.20
      java-version: 17